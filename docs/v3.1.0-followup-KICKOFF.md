# build-it v3.1.0 Follow-up — Kickoff Note

Orientation for a FRESH session finishing the fable-it → build-it rename.
Read this first, then `.build-it-reports/report-v3.1.0-rename.md` (the rename
run's full report) and `.taskstate/decisions.md` (D5–D11). This note carries
the context from the rename session that those files don't.

## What you're building (one line)
Regenerate the brand assets whose pixels still say "fable-it", after a quick
sanity check that the renamed setup is healthy.

## First concrete slice (do this first)
Phase 1 sanity check, before any design work: `git remote -v` shows
`DevOtts/build-it`; `bash tests/lints/run-all.sh` is green; `claude plugin
marketplace update devotts` succeeds (it follows the GitHub redirect);
`/build-it` routes and `/fable-it` loads the deprecated pointer.
Anti-pattern: do NOT "fix" historical artifacts that still say fable-it
(old reports, docs/research, delivery/, eval runs, past CHANGELOG entries) —
they are records, kept old on purpose (decision D6).

## Where the code lives
- Repo: `~/Workspace/Devotts/build-it` (this session assumes Fernando already
  ran `mv ~/Workspace/Devotts/fable-it ~/Workspace/Devotts/build-it`; if the
  folder is still `fable-it`, stop and say so).
- Remote: https://github.com/DevOtts/build-it, branch `main`, released tag
  `v3.1.0` (rename commit `1d78426`).
- Assets to regenerate: `assets/build-it-header.png`, `assets/build-it-header.gif`,
  `assets/build-it-animation.mp4`, `assets/build-it-flow.png`,
  `build-it-field-guide.pdf` (repo root).
- README references these paths already — keep the filenames, replace the pixels.

## The locked decisions
1. Name is **build-it** (Fernando confirmed; family convention with plan-it /
   review-it / prompt-it). Never "built-it".
2. Tagline is **"plan-it plans it, build-it builds it."** — the old "Make your
   model behave like Fable" is retired from branding (t20 lint now enforces
   this), but Fable-the-model references in README body/research stay.
3. Historical artifacts keep the old name (D6) — records, not docs.
4. Back-compat is deliberate: `/fable-it` triggers, `fable-it` keyword,
   `FABLE_IT_HOOKS_DISABLED`, legacy `.fable-it-reports/lessons.md` reads.
   Don't remove any of it while "cleaning up".

## Suggested phase order
1. Sanity check (above) — cheap, catches redirect/cache surprises.
2. Header set: `build-it-header.png` + `.gif` (+ `animation.mp4` if feasible)
   with the build-it wordmark — consider `/banner-design` + `/brand`.
3. `build-it-flow.png` flow diagram re-labeled.
4. `build-it-field-guide.pdf` — note this was ALREADY stale before the rename
   (open debt G2.4 from v2); decide with Fernando whether to regenerate or drop
   it from the README.
5. Commit + push; optionally a v3.1.1 chore release if the README changes.

## How to run + test (this session's setup)
- Lint suite: `bash tests/lints/run-all.sh` → expect "ALL LINTS + UNIT TESTS
  GREEN" (9/9). t20 derives version pins from CHANGELOG top entry.
- Marketplace refresh: `claude plugin marketplace update devotts`.
- No services, no env vars.

## Gotchas to carry forward (bit us this session)
- How the ORIGINAL assets were generated is unknown (git history only shows
  "Enhance documentation") — check `/banner-design` & `/ai-image-prompts-skill`
  or ask Fernando before reverse-engineering a style.
- Project memory is path-keyed: it was pre-staged at
  `~/.claude/projects/-Users-macbook-Workspace-Devotts-build-it/memory/`, so
  the renamed folder recalls everything. The old key still exists; don't be
  confused by the duplicate.
- plan-it's `version-triple-match.mjs` reads the marketplace entry BY NAME now
  (was plugins[0]) — keep that if touching plan-it.
- prompt-it eval refresh (`eval/` still says /fable-it) is OUT of scope here —
  it belongs to the next prompt-it release session, in the prompt-it repo.

## Handoff state
- DONE + pushed: full rename across 6 repos, v3.1.0 release, local skills +
  deprecated `/fable-it` pointer, memory updated. Report:
  `.build-it-reports/report-v3.1.0-rename.md`.
- OPEN: the four asset files above (pixels), field-guide decision (regenerate
  vs drop — owed to Fernando), folder rename itself (Fernando runs the `mv`).
